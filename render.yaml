services:
  - type: web
    name: linkmagico-site
    runtime: node
    plan: free
    buildCommand: npm install && node scripts/update-stats.js
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
